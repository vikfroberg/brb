#!/usr/bin/env node

var Notification = require('node-notifier');

var notifier = new Notification();
notifier.notify({
    title: 'Terminal task completed',
    message: 'Come back!',
    sound:true
});
